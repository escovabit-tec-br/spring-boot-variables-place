spring:
  application:
    name: spring-boot-variables-place
  cloud:
    config:
      uri: ${SPRING_CONFIG_URI:http://localhost:8888}

app:
  config:
    phone:
      notPhone:
        msg: 'notPhone'
